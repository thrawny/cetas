		console.log(myEnum.get('A').value);
		console.log('postdata');
		Article.create([{ title: myEnum.get('A').value, text: myEnum.get('B').value}],
		
		
		
		        each patient in list
            li.patient 
                h2
                    = patient.user                         
                ul.recordList  
                    h3.recordListTitle Inlämnade formulär
                        each record in patient.records
                            li 
                                a(href='/mypatients/records/'+record.id) 
                                    = record.id
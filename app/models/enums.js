/**
 * New node file
 */
"use strict";

var Enum = require('enum');

var yesOrNo = new Enum({
	'yes' : 'Yes',
	'no' : 'No'
});

var pain = new Enum({
	'1' : 'Ingen smärta',
	'2' : 'Neutral',
	'3' : 'Värsta tänkbara'
});

var myEnum = new Enum({
	'A' : '1',
	'B' : '2',
	'C' : 4
});

var myEnum = new Enum({
	'A' : '1',
	'B' : '2',
	'C' : 4
});